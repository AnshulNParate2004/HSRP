#!/bin/sh
# Strip Cursor co-author trailers so cursoragent is not attributed on GitHub.
COMMIT_MSG_FILE="$1"
if [ -f "$COMMIT_MSG_FILE" ]; then
  grep -vi '^Co-authored-by:.*[Cc]ursor' "$COMMIT_MSG_FILE" > "${COMMIT_MSG_FILE}.tmp" || true
  mv "${COMMIT_MSG_FILE}.tmp" "$COMMIT_MSG_FILE"
fi
