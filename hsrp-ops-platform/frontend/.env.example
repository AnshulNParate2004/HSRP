# =============================================================================
# Real Mazon HSRP — Frontend Environment
# Copy to: frontend/.env
# =============================================================================

# -----------------------------------------------------------------------------
# DEVELOPMENT (npm run dev — Vite on port 8080, proxy to backend :8000)
# -----------------------------------------------------------------------------
# Leave VITE_API_URL empty — the app uses /api/v1 and Vite proxies to backend.
# VITE_API_URL=

# -----------------------------------------------------------------------------
# PRODUCTION — Option A: Single server (npm run start)
# Backend serves frontend from port 8000 on same origin.
# No .env needed — default /api/v1 works.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# PRODUCTION — Option B: Split hosting (frontend CDN + API on different domain)
# Set full API prefix including /api/v1
# -----------------------------------------------------------------------------
# VITE_API_URL=https://api.realindustries.in/api/v1
